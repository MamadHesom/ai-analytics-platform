document.addEventListener('DOMContentLoaded',()=>{document.querySelector('.menu-btn')?.addEventListener('click',()=>document.querySelector('.nav-links')?.classList.toggle('mobile-open'));const button=document.getElementById('new-pipeline');button?.addEventListener('click',async()=>{const toast=document.getElementById('toast');try{if(typeof createPipeline==='function')await createPipeline();toast.textContent='Pipeline created successfully.';}catch(e){toast.textContent='Demo mode: pipeline queued locally.'}toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3000);});});
